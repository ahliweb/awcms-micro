# awcms-micro-dev

This directory is a managed local copy of the latest EmDash repository.

Refresh it with:

```bash
pnpm refresh:awcms-micro-dev
```

By default the refresh script clones the latest upstream `emdash-cms/emdash`
repository and replaces the contents of this directory.
