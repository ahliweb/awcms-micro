---
"@awcms-micro/plugin-sikesra": patch
---

Allow trusted EmDash admins to bootstrap SIKESRA admin access and fall back safely when production D1 SIKESRA tables are not available yet, so protected registry, custom attribute, audit, archive, delete request, access, ABAC, region, data type, and public status pages can load instead of failing.
