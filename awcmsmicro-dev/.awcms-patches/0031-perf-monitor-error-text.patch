diff --git a/infra/perf-monitor/public/index.html b/infra/perf-monitor/public/index.html
index 06d8fd9..2462d73 100644
--- a/infra/perf-monitor/public/index.html
+++ b/infra/perf-monitor/public/index.html
@@ -1031,9 +1031,13 @@
 					// Auto-refresh every 5 minutes
 					setInterval(refresh, 5 * 60 * 1000);
 				} catch (err) {
-					document.querySelector(".container").innerHTML = `
-            <div class="error-msg">Failed to load: ${err.message}</div>
-          `;
+					const container = document.querySelector(".container");
+					const errorMessage = err instanceof Error ? err.message : "Unknown error";
+					const errorEl = document.createElement("div");
+					errorEl.className = "error-msg";
+					errorEl.textContent = `Failed to load: ${errorMessage}`;
+					container.textContent = "";
+					container.append(errorEl);
 				}
 			}
 
