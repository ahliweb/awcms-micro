---
"awcms-micro": patch
---

Commit the `graphify-out/` knowledge-graph output (generated by the `/graphify` skill) to the repository at the operator's request. Generated/regenerable artifacts (`graph.json`, `graph.html`, `manifest.json`, `GRAPH_REPORT.md`, `cost.json`, and the `cache/` AST index); no runtime code or behavior change.
