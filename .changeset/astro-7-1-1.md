---
"awcms-micro": patch
---

Bump `astro` from 7.1.0 to 7.1.1 (patch). Upstream patch release; no application contract, schema, endpoint, or runtime-behavior change on our side — the dependency update is covered by the full CI suite (typecheck, unit/integration, E2E, build).
